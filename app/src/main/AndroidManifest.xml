<?xml version="1.0" encoding="utf-8"?>
<manifest
    package="com.fernandocejas.android10.rx.sample"
    xmlns:android="http://schemas.android.com/apk/res/android">

  <application
      android:allowBackup="true"
      android:icon="@drawable/ic_launcher"
      android:label="@string/app_name"
      android:theme="@style/AppTheme">
    <activity
        android:name=".activity.MainActivity"
        android:label="@string/app_name">
      <intent-filter>
        <action android:name="android.intent.action.MAIN" />
        <category android:name="android.intent.category.LAUNCHER" />
      </intent-filter>
    </activity>

    <activity
        android:name=".activity.SampleActivityObserver"
        android:label="@string/app_sample_observer" />

    <activity
        android:name=".activity.SampleActivityBackpressure"
        android:label="@string/app_sample_observable" />


    <activity
        android:name=".activity.SampleActivityObservableTransfor"
        android:label="@string/app_sample_observable_transform" />

  </application>

</manifest>
